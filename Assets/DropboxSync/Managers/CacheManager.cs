using System.IO;
using System.Linq;

namespace DBXSync {

    public class CacheManager {

        private DropboxSyncConfiguration _config;

        public CacheManager(DropboxSyncConfiguration config){
            _config = config;
        }

        
    }
}